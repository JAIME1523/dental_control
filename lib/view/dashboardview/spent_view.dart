import 'package:flutter/material.dart';

class SpentView extends StatelessWidget {
   
  const SpentView({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Text('SpentView');
  }
}