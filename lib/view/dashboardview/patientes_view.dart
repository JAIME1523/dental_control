import 'package:flutter/material.dart';

class PatientesView extends StatelessWidget {
   
  const PatientesView({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Text('PatientesView');
  }
}